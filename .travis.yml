language: node_js
node_js:
  - "10.4"

before_install:
  - yarn travis

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: master
  local_dir: $TRAVIS_BUILD_LOCATION/dist